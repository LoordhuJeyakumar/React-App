/* const initialUserFilgterState = {
  userID: 0,
  nameFilter: [],
};

function userFilterReducer(state, action) {
    console.log(action.payLoad.value);
  switch (action.type) {
    case "userID": {
      return {
        ...state,
        nameFilter: action.payLoad.value
      };
    }
    default: {
      return state;
    }
  }
}

export { initialUserFilgterState, userFilterReducer };
 */